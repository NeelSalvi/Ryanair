'use strict'

const CONSTANTS = {
  STEP_TIMEOUTS: {
    TIMEOUT: 6000,
  },

  HOOK_TIMEOUTS: {
    BEFORE: 6000,
    AFTER: 6000,
  },

  ELEMENT_TIMEOUT: {
    TIMEOUT: 20000,
    RM_TIMEOUT: 10000,
  },
}

module.exports = CONSTANTS
